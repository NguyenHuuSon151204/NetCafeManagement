<UserControl x:Class="NetCafeManagement_WDF.AdminControl.ActivityLogControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Background="#F5F7FA">

    <UserControl.Resources>
        <!-- Filter Button Style -->
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" 
                Padding="20" Margin="0,0,0,15" 
                BorderBrush="#E1E5E9" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Border Background="#8B5CF6" CornerRadius="8" 
                        Width="50" Height="50" Margin="0,0,15,0">
                    <TextBlock Text="📊" FontSize="24" 
                             HorizontalAlignment="Center" 
                             VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Lịch sử hoạt động" 
                             FontSize="24" FontWeight="Bold" 
                             Foreground="#1F2937"/>
                    <TextBlock Text="Theo dõi các hoạt động của người dùng trong hệ thống" 
                             FontSize="13" Foreground="#6B7280" 
                             Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Filter Section -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" 
                Padding="20" Margin="0,0,0,15"
                BorderBrush="#E1E5E9" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Filter Buttons -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Lọc theo:" VerticalAlignment="Center" 
                             Margin="0,0,15,0" FontWeight="SemiBold" Foreground="#374151"/>

                    <Button Content="Tất cả" Click="AllFilter_Click" 
                            Style="{StaticResource FilterButtonStyle}"/>
                    <Button Content="Đăng nhập" Click="LoginFilter_Click" 
                            Style="{StaticResource FilterButtonStyle}"/>
                    <Button Content="Đăng xuất" Click="LogoutFilter_Click" 
                            Style="{StaticResource FilterButtonStyle}"/>
                    <Button Content="Thanh toán" Click="PaymentFilter_Click" 
                            Style="{StaticResource FilterButtonStyle}"/>
                </StackPanel>

                <!-- Date Range and Actions -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="Từ ngày:" VerticalAlignment="Center" 
                             Margin="0,0,10,0" FontWeight="SemiBold" Foreground="#374151"/>
                    <DatePicker x:Name="FromDatePicker" Width="120" Height="35" 
                              Margin="0,0,15,0" SelectedDate="{x:Static sys:DateTime.Today}"/>

                    <TextBlock Text="Đến ngày:" VerticalAlignment="Center" 
                             Margin="0,0,10,0" FontWeight="SemiBold" Foreground="#374151"/>
                    <DatePicker x:Name="ToDatePicker" Width="120" Height="35" 
                              Margin="0,0,20,0" SelectedDate="{x:Static sys:DateTime.Today}"/>

                    <Button Content="🔍 Tìm kiếm" Click="SearchButton_Click" 
                            Background="#3B82F6" Foreground="White"
                            Style="{StaticResource ActionButtonStyle}"/>
                    <Button Content="📄 Xuất Excel" Click="ExportButton_Click" 
                            Background="#10B981" Foreground="White"
                            Style="{StaticResource ActionButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Activities -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" 
                    Padding="15" Margin="0,0,7.5,0"
                    BorderBrush="#E1E5E9" BorderThickness="1">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Background="#EEF2FF" CornerRadius="6" 
                                Width="32" Height="32" Margin="0,0,10,0">
                            <TextBlock Text="📈" FontSize="16" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Tổng hoạt động" FontSize="12" 
                                     Foreground="#6B7280" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TotalActivitiesText" Text="0" 
                                     FontSize="18" FontWeight="Bold" 
                                     Foreground="#1F2937"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Today's Logins -->
            <Border Grid.Column="1" Background="White" CornerRadius="10" 
                    Padding="15" Margin="7.5,0,7.5,0"
                    BorderBrush="#E1E5E9" BorderThickness="1">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Background="#D1FAE5" CornerRadius="6" 
                                Width="32" Height="32" Margin="0,0,10,0">
                            <TextBlock Text="🔑" FontSize="16" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Đăng nhập hôm nay" FontSize="12" 
                                     Foreground="#6B7280" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TodayLoginsText" Text="0" 
                                     FontSize="18" FontWeight="Bold" 
                                     Foreground="#10B981"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Active Users -->
            <Border Grid.Column="2" Background="White" CornerRadius="10" 
                    Padding="15" Margin="7.5,0,7.5,0"
                    BorderBrush="#E1E5E9" BorderThickness="1">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Background="#DBEAFE" CornerRadius="6" 
                                Width="32" Height="32" Margin="0,0,10,0">
                            <TextBlock Text="👥" FontSize="16" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Đang hoạt động" FontSize="12" 
                                     Foreground="#6B7280" FontWeight="SemiBold"/>
                            <TextBlock x:Name="ActiveUsersText" Text="0" 
                                     FontSize="18" FontWeight="Bold" 
                                     Foreground="#3B82F6"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Today Revenue -->
            <Border Grid.Column="3" Background="White" CornerRadius="10" 
                    Padding="15" Margin="7.5,0,0,0"
                    BorderBrush="#E1E5E9" BorderThickness="1">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Background="#FEE2E2" CornerRadius="6" 
                                Width="32" Height="32" Margin="0,0,10,0">
                            <TextBlock Text="💰" FontSize="16" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Doanh thu hôm nay" FontSize="12" 
                                     Foreground="#6B7280" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TodayRevenueText" Text="0 VNĐ" 
                                     FontSize="18" FontWeight="Bold" 
                                     Foreground="#EF4444"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- DataGrid -->
        <Border Grid.Row="3" Background="White" CornerRadius="10" 
                BorderBrush="#E1E5E9" BorderThickness="1">
            <DataGrid x:Name="ActivityLogDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      Background="White"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      FontSize="13"
                      RowHeight="45" SelectionChanged="ActivityLogDataGrid_SelectionChanged">

                <!-- Header Style -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <!-- Row Style -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <!-- Cell Style -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EEF2FF"/>
                                <Setter Property="Foreground" Value="#1F2937"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    

                    <DataGridTextColumn Header="Người dùng" Binding="{Binding Username}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#1F2937"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Loại hoạt động" Binding="{Binding ActivityType}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActivityType}" Value="Login">
                                        <Setter Property="Foreground" Value="#10B981"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ActivityType}" Value="Logout">
                                        <Setter Property="Foreground" Value="#EF4444"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ActivityType}" Value="Payment">
                                        <Setter Property="Foreground" Value="#3B82F6"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Máy tính" Binding="{Binding ComputerName}" Width="100"/>

                    <DataGridTextColumn Header="Thời lượng" Binding="{Binding Duration}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#059669"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Số tiền" Binding="{Binding Amount}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#DC2626"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Thời gian" Binding="{Binding Timestamp}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#374151"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>