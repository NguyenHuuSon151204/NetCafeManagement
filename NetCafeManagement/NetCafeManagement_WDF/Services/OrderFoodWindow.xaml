<Window x:Class="NetCafeManagement_WDF.Views.OrderFoodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gọi món" Height="500" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="ProductGrid" AutoGenerateColumns="False" CanUserAddRows="False" Grid.Row="0"
                  ItemsSource="{Binding}" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên món" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Loại" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Giá (VNĐ)" Binding="{Binding Price}" Width="*"/>
                <DataGridTemplateColumn Header="Số lượng" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Tổng tiền: " VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock x:Name="TotalAmountText" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="✔️ Đặt món" Width="120" Click="ConfirmOrder_Click"/>
        </StackPanel>
    </Grid>
</Window>
